/**
 * Abstract class which defines what all SynergyMesh items should do.
 * (e.g. respond to gesture, manage being transfered, etc.)
 */
export abstract class SynergyMeshItem {

    /**
     * Initialise a SynergyMeshItem object.
     */
    public constructor() {
		
	}
	
}
