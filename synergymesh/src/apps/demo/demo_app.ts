import {SynergyMeshApp} from 'apps/synergymesh_app';
  
 /**
 * Abstract class which defines what all SynergyMesh apps should do.
 * (e.g. load content, connect to other instances, etc.)
 */
export class DemoApp extends SynergyMeshApp {

    /**
     * Initialise a DemoApp object.
     */
    public constructor() {
		super();
	}
	
}
